import * as React from "react"
import {Button} from "@mui/material"

const Demo = () => {
    return(
        <Button variant="contained" disableElevation>Just a demo button</Button>
    )
};

export default Demo